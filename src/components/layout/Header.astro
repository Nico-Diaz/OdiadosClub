<header class="main-header">
    <div class="logo font-gothic">ODIADOS CLUB</div>
    <nav>
        <button class="btn-order">PEDIR YA</button>
    </nav>
</header>

<style>
  .main-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background-color: rgba(10, 10, 10, 0.95);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    color: var(--color-red);
    font-size: 1.5rem;
  }

  .btn-order {
    background-color: var(--color-white);
    color: var(--color-black);
    border: none;
    padding: 0.5rem 1rem;
    font-weight: bold;
    font-family: var(--font-sans);
    text-transform: uppercase;
    cursor: pointer;
    transform: skewX(-10deg); /* Efecto inclinado veloz */
  }
</style>